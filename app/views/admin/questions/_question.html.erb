<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('shared.show'), admin_question_path(question) %></td>
  <td><%= link_to t('shared.edit'), edit_admin_question_path(question) %></td>
  <td>
    <%= link_to t('shared.delete'),
              admin_question_path(question),
              method: :delete,
              data:   { confirm: 'Are you sure?' } %>
  </td>
</tr>
