<h1><%= t('.header') %></h1>
<% if user_signed_in? %>
  <p>
    <%= link_to t('.new_test'), new_admin_test_path %>
  </p>

  <table>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>

    <%= render @tests %>
  </table>
<% end %>
